# Configuration for Cog
# https://cog.run/yaml

build:
  gpu: true
  cuda: "12.1"
  python_version: "3.11"
  python_packages:
    - "torch==2.4.0"
    - "torchaudio==2.4.0"
    - "transformers>=4.45.0"
    - "outetts>=0.3.0"
    - "huggingface_hub>=0.25.0"

predict: "predict.py:Predictor"
